// RBAC.jsx
import React from 'react';

const RBAC = () => {
  return (
    <div className="module-box bg-red-900 p-4 rounded-2xl shadow-lg">
      <h2 className="text-xl font-semibold mb-2">RBAC (Access Control)</h2>
      <p className="text-sm opacity-80">
        Role-Based Access Control manager for user layers. Controls admin/user
        level privileges, system command scope, and interactive authorization
        states.
      </p>
    </div>
  );
};

export default RBAC;
